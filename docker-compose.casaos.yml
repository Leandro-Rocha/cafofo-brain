services:
  cafofo-brain:
    image: ghcr.io/leandro-rocha/cafofo-brain:latest
    container_name: cafofo-brain
    restart: unless-stopped
    env_file:
      - /etc/cafofo/brain.env
    volumes:
      - brain-data:/app/data
    ports:
      - "3030:3030"

volumes:
  brain-data:
